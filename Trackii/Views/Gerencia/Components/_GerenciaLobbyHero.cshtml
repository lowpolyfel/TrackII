@model Trackii.Models.GerenciaLobby.GerenciaLobbyVm

<header class="gerencia-lobby-hero">
    <p class="gerencia-lobby-kicker">Nuevo lobby</p>
    <h2>@Model.Title</h2>
    <p>@Model.Subtitle</p>
</header>
